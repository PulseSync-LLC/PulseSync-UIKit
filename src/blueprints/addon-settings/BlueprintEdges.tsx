/**
 * @deprecated Use VisualBlueprintEditor which now uses ReactFlow's edges internally.
 * This component is kept for backward compatibility.
 */
export function BlueprintEdges() {
    return null
}
