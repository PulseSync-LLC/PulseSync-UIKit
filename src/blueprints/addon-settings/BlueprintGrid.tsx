/**
 * @deprecated Use VisualBlueprintEditor which now uses ReactFlow's Background internally.
 * This component is kept for backward compatibility.
 */
export function BlueprintGrid() {
    return null
}
